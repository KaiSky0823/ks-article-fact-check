---
name: ks-article-fact-check
description: 对文章或公众号 Markdown 稿件做全维度事实核查。用于用户要求核对文章中的人物、头衔、引语、数字、事件、时间线或出处，并希望对照原始信源、外部权威信源，生成核查报告和不改文风的 fact-checked 新稿时。
---

# 文章事实核查

把事实准确性和文风修改分开处理。保留原稿，另存报告和修正稿。

## 输入

- 必填：待核查 Markdown 稿路径。若路径也未提供，把稿件路径和原始信源位置放在同一个简短问题中询问，避免来回确认。
- 原则上需要：原始报道、PDF、本地资料或链接。若用户未提供，先询问原始信源位置；若用户明确要求只做外部核查，记录这一限制后继续。

## 工作流

### 1. 建立声明清单

完整读取稿件，逐条提取可核验声明：

- 人物、机构与头衔；
- 带引号或明确归属的转述；
- 数字、比例、页数、金额和持续时间；
- 事件、地点和时间线；
- 报道出处及发布日期。

主观评价和显然属于修辞的表达不列为事实错误，但要标出容易被误读为事实的边界情况。

### 2. 做两路独立核查

当并行 agent 可用时，同时发起两项任务；让两个 agent 分别写入不同文件，避免写入冲突：

- 原始信源核查：逐条对照稿件和用户提供的原始材料，给出 `✅/⚠️/❌`、证据摘录及材料位置，重点检查归属、数字、顺序和头衔。写入 `{article-dir}/fact-check-source.md`。
- 外部信源核查：浏览 Web，用第一方资料、机构官网、原始论文/数据和高质量报道验证仍需外部确认的声明。对关键结论尽量使用两个独立权威来源，记录链接和核查日期。写入 `{article-dir}/fact-check-external.md`。

不要把预计结论或疑点答案泄露给核查 agent。若当前环境不支持并行 agent，则在主线程做两次相互独立的核查，并保持两份报告分离。

### 3. 合并与分级

读取两份报告，按以下级别处理：

- 必改硬错误：错误归属、头衔、数字、时间、机构，或引号内并非原话。直接修正。
- 轻度偏差：相对时间不精确、统计口径不一致、最高级表述证据不足。列出建议和依据；除非事实明显错误，不擅自改变作者立场。
- 完全有据：保留。
- 未能核实：明确写“未找到足够证据”，不得补写或猜测。

将合并报告写入 `{article-dir}/fact-check-summary.md`。若目标文件已经存在，不覆盖旧报告；改用带时间戳或递增后缀的新文件名。每项结论都要能追溯到本地材料位置或网页链接。

### 4. 输出修正稿

只修事实，不顺手润色修辞。保留原文件，输出 `{article-dir}/draft4-fact-checked.md`；若原稿命名不适合该名称，使用清晰的 `*-fact-checked.md`。目标已存在时同样使用新后缀，不静默覆盖。

最终报告：

- `✅/⚠️/❌/未核实` 数量；
- 必改项的 before/after；
- 建议改动；
- 修正稿绝对路径；
- 合适的下一步。

不要省略具体声明，不要伪造引用，不要把“没有搜到”写成“已证伪”。
